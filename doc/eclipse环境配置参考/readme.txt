本目录下的.classpath文件和.settings目录为该项目必需文件，但由于多人开发，
各人的本地环境有所不同，造成每次提交代码这两项都会冲突，为避免冲突，将这
两项加入了.gitignore文件中，避免每次提交，将该两项文件放此处备份，并将文
件和目录名称前的"."取消。

如果是从服务端重新clone该项目，则在下载完成后，将该两项拷贝至本项目的根目
录下，并将文件名恢复为正确名称，以使项目能正常使用。